# _archive/ — 旧事務所向けデモ資産

このディレクトリには、KessanPilot が「会計事務所向け BtoB SaaS デモ」だった頃の資産を退避しています。
自社1社の経理運用ツールへ転換した現プロジェクトでは直接使いませんが、UIパーツやアイデアの参照用に残しています。

## 内容

| パス | 元の役割 |
|---|---|
| `biz/` | 法人マイページ「DriveKeiri」のデモ一式 |
| `client-report.html` | 顧問先（社長）が受け取る月次レポート画面 |

## 復活させたい場合
ここから親ディレクトリへ戻せば動作します。ただし `js/mock-data.js` のモックデータに依存しているため、
本体側がデータレイヤーを `data/*.json` に移行した後は単体では動かなくなります。
